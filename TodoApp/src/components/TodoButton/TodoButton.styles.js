import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 5,
    height: 40,
    justifyContent: 'center',
  },
  button_text: {
    textAlign: 'center',
    color: '#fefefe',
  },
});
